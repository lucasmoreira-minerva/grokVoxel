{
  "name": "grokstage",
  "version": "0.3.0",
  "private": true,
  "description": "GrokStage — SuperGrok video director (storyboard-first, skill styles, headless engine)",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "start": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
